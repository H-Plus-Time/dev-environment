# Get nvm
# Unzip nvm
# Get uru
# Get anaconda
# Set a bunch of environment variables
# Shout at the user to reboot
# Alternatively, kill all explorer.exe instances, start a new one - check if this works for nvm, instructions were given to that effect.
conda env update ../envs/base.yml
